









self.memory.sample(self.batch_size)

self.process_state_batch--------np.array

self.model.predict_on_batch

self.trainable_model.train_on_batch

current_terminal = self.terminals[current_idx - 1]

                state0.append(self.observations[current_idx])


